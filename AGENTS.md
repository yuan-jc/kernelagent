# 开发约束与跨会话交接

本项目目标：自动、证据驱动、面向对象的 NVIDIA 算子优化 agent，优先复用现有工具。

## 读取与范围

- 设计基线：`docs/NVIDIA算子优化Agent设计文档.md`。
- 开发任务与验收：`docs/开发任务与验收计划.md`；状态：`docs/task-board.json`；交接：`docs/handoffs/current.md`。
- 开始时只读取当前工作包、交接、相关设计章节和源码。不要重复全量调研。
- 用户指定工作包时只推进该包及必要修复；未指定时先按依赖确认当前任务，不一次实现整个系统。
- 不主动创建其他 Codex 任务或启用开发子 agent；用户明确要求时再使用。

## 不变约束

- Domain 不依赖 torch、Triton、模型 SDK 或具体 adapter；benchmark、backend、硬件和搜索策略用组合接口。
- 复用 KernelBench 等原生任务/评测与工具，新增能力通过适配器接入，不维护一份平行题库或无必要的工具复制。
- 候选不能决定自己的正确性、计时成绩或晋升；可信 evaluator/父服务持有结果与规则。
- 正式 timing 与 profiling 分开；缺失指标不是零；SOL 不证明算法最优。
- 晋升至少绑定代码/环境/协议身份、正确性和独立性能确认；机制和因果主张需要对应证据。
- 清单、容差、允许依赖、计时范围、统计分母不能为过测而改变；扩展评测单独报告。
- 真实 GPU/模型检查未执行时标记 NOT_RUN，不能用 mock、skip 或自述替代完成。
- 搜索、调参、修复和 profiling 均计入预算；中断可恢复；错误/无改进是合法结果。

## 每包收尾

- 实现前明确可运行验收命令及正反例，按工作包所需层级验证。
- 不引入 no-op/TODO 来冒充本包功能；检查 0 项或全 skip 不算通过。
- 更新 task board、交接和证据路径，记下失败检查和活动作业；无需等额度耗尽再保存。
- `ACCEPTED` 要有实际检查证据。GPU 包没有 GPU 验收最多为 READY_FOR_ACCEPTANCE。
- 关键接口/协议偏离写 ADR；普通实现细节自行处理。不要因常规检查点重复请求许可。
- 最终报告说明行为变化、验证及未验证范围，不把文档或类骨架当完整 agent。
