# sm_89 solutions (project-authored, T19)

torch return-style and DPS solutions in FlashInfer-Bench solution format,
restricted to sm_89. The upstream example solution
(example_agent_solution.json, fetched under research/sources/) targets
B200 and is used as the hardware-guard negative case.
