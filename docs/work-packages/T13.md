# T13：复用/模板方法

## 契约

- 状态：ACCEPTED（2026-09-13，目标真机 GPU 验收）。依赖 T02（ACCEPTED）、T10（ACCEPTED）、T12（READY_FOR_ACCEPTANCE：生成循环机制已验收，LIVE_MODEL 待用户凭据——本包两方法均不依赖在线模型）。
- 对应设计章节：§6.1–6.3。
- 目标行为：Method 注册表 + Reuse/Template 两种生成器走同一主循环；域不匹配/许可/依赖白名单三道门显式拒绝；候选带来源（方法名、许可、依赖、sha256）且必须经 T05 评估器独立复验。

## 验收矩阵

| 检查 ID | 层级 | 精确命令/审查入口 | 预期观察与失败条件 | 实际状态 | 证据路径 |
|---|---|---|---|---|---|
| M1 双生成器同循环 | GPU | `uv run --locked python examples/methods_smoke.py --output artifacts/t13` | template 与 reuse 方法经同一循环产候选，均通过真实 GPU 独立复验 | PASS（各 gpu_verified=True） | artifacts/t13/methods-report.json |
| M2 域不匹配拒绝 | CPU | 同上（refusals 段）+ `pytest tests/test_methods.py` | matmul 方法对 layernorm 任务被拒并给出原因 | PASS | 同上 |
| M3 许可/依赖白名单 | CPU | `uv run --locked pytest tests/test_methods.py` | 未接受许可的方法拒绝注册；候选越界导入依赖被拒 | PASS（8 项） | artifacts/t1314-cpu |
| M4 候选独立复验 | GPU | M1 | 生成器声明不构成正确性；全部候选经 pinned 上游 evaluator | PASS | artifacts/t13/methods-report.json |
| R 回归 | CPU | `uv run --locked kernelagent check --output artifacts/t1314-cpu` | 全量检查不回归 | PASS（406 项 403 过 / 3 Windows 专用 skip / 0 fail） | artifacts/t1314-cpu/a4b8ce183ecb439e8147038d2b4a4017/report.json |

## 运行证据

- 代码/输入/环境/协议身份：`src/kernelagent/methods.py`；任务 l1 p040 LayerNorm；GPU RTX 4060 Laptop（CDI UUID）；镜像 `sha256:cb7a9f4c…`。
- 执行命令及退出码：冒烟 exit 0（accepted=True）；单测 exit 0（8 项）；R exit 0。
- 原始日志/报告 hash：报告 sha256（见证据索引 target_ubuntu_t13）；R 报告 sha256 `8f518b7582f5033661556c1c565c8572d8e749cc21a99761c6069cd59c929fe9`。
- skip 数量和原因：3 项 Windows Job Object 专用（Linux skip，同前）。
- 活动作业与恢复方式：无持续作业。

## 结论

- 是否满足本包全部必需检查：是。
- 与设计的偏差及 ADR：无。
- 仍未验证的范围：与 LLM 生成器组合的 CompositeGenerator 属 T16（本包两方法均为确定性生成器）。
- 下一步：T14 原生参数调优。
