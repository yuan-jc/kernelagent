# T12：模型客户端与初始生成

## 契约

- 状态：READY_FOR_ACCEPTANCE（2026-09-13：除 LIVE_MODEL 外全部检查 PASS；真实闭环需用户在控制端配置 provider 凭据）。依赖 T01（ACCEPTED）、T04（ACCEPTED）、T10（ACCEPTED）；T12a（离线层）ACCEPTED。
- 对应设计章节：§5–6、§12；计划 §48（真实闭环允许冻结 smoke 问题；失败即失败，不得以回放替代）。
- 实现：`openai_compat.py`（stdlib 传输、限次重试语义、429/5xx=transient、401/403/400=permanent、未配置凭据=显式 permanent）；`generation.py`（冻结 prompt 模板、request hash 身份、结构校验（语法+ModelNew）、CostLedger 计费、失败记录显式）；复用 T12a 的 Recorded/Retrying/Scripted 客户端。
- LIVE_MODEL 协议：凭据仅经控制端环境变量 `MODEL_PROVIDER_API_KEY`（+ `--base-url/--model-id`），绝不写入仓库、日志或 worker 容器。

## 验收矩阵

| 检查 ID | 层级 | 精确命令/审查入口 | 预期观察与失败条件 | 实际状态 | 证据路径 |
|---|---|---|---|---|---|
| G1 录制闭环：模型→代码→GPU | GPU | `uv run --locked python examples/generation_loop_smoke.py --output artifacts/t12` | 录制"好"响应生成的候选经 T04 容器边界在真实 GPU 上由上游 evaluator 判正确 | PASS（gpu_verified=True） | artifacts/t12/generation-loop-report.json |
| G2 错误响应不产生成功 | CPU | 同上 | 拒绝式响应 → stage=parse 失败记录；账本仍计费 | PASS | 同上 |
| G3 凭据只在控制端 | REVIEW | 代码审查 + 容器 env 检查 | 凭据不进容器 env/日志/仓库；未配置时 live 调用被策略拒绝 | PASS | examples/generation_loop_smoke.py（live 分支） |
| G4 异常分支（录制） | CPU | `uv run --locked pytest tests/test_generation_loop.py` | 解析失败/结构缺失/传输失败分类正确；请求 hash 稳定且对 seed 敏感；拒绝输出仍计费 | PASS（5 项） | artifacts/t12-cpu |
| G5 LIVE_MODEL 真实闭环 | LIVE_MODEL | `MODEL_PROVIDER_API_KEY=… uv run … generation_loop_smoke.py --output artifacts/t12 --live` | 真实 provider 生成→代码→GPU 判正确一次 | **NOT_RUN（阻塞：需用户提供凭据）** | — |
| R 回归 | CPU | `uv run --locked kernelagent check --output artifacts/t12-cpu` | 全量检查不回归 | PASS（390 项 387 过 / 3 Windows 专用 skip / 0 fail） | artifacts/t12-cpu/a1dfc1714cf1456d9ea5cc86c21c7811/report.json |

## 运行证据

- 代码/输入/环境/协议身份：`src/kernelagent/adapters/models/{openai_compat,generation}.py`；录制响应内容 = 已验证的 T07 Triton 夹具（JSON 封装）；GPU RTX 4060 Laptop（CDI UUID）；协议 t12-generation-loop-v1。
- 执行命令及退出码：GPU 冒烟 exit 0（generated/gpu_verified/refusal_rejected 全 True，live=not_run）；R exit 0；G4 exit 0（5 项）。
- 原始日志/报告 hash：报告 sha256 `2b189d8c85ee5042d52c665b80f294143e87eafa41c142fdfe54b410dc50e1f4`；R 回归报告 sha256 `ac54414bd050ece5e10e6b6467f753bb91192e6091f3e8a041a73e85db1079b1`。
- skip 数量和原因：3 项 Windows Job Object 专用（Linux skip，同前）；LIVE_MODEL NOT_RUN=需用户凭据（非 skip，是外部阻塞）。
- 活动作业与恢复方式：无持续作业；用户提供凭据后 `--live` 即完成 G5。

## 结论

- 是否满足本包全部必需检查：除 G5（LIVE_MODEL）外全部 PASS。机器可证的循环机制（含错误分支、计费、GPU 验证）已完备并有证据。
- 与设计的偏差及 ADR：无。
- 仍未验证的范围：G5 真实 provider 闭环——等价于计划 §48 的"LIVE_MODEL 未运行必须 NOT_RUN"，不伪造。
- 解除阻塞所需用户动作：提供任一 OpenAI 兼容 provider 的 base_url + API key（导出 `MODEL_PROVIDER_API_KEY` 后以 `--live` 重跑 G5），随后本包可转 ACCEPTED。
