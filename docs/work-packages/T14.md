# T14：原生参数调优

## 契约

- 状态：ACCEPTED（2026-09-13，目标真机 GPU 验收）。依赖 T07（ACCEPTED）、T10（ACCEPTED）、T13（ACCEPTED）。
- 对应设计章节：§8.4（编译与 autotune 的项目内补充）。
- 目标行为：Triton 配置扫描适配器——结构非法配置预剪枝（不进测量）、逐配置正确性验证（不正确不得成为 winner）、in-place 候选的输入逐批恢复（事件计数）、逐配置原始批样本入账、winner 冻结后新运行复验。

## 验收矩阵

| 检查 ID | 层级 | 精确命令/审查入口 | 预期观察与失败条件 | 实际状态 | 证据路径 |
|---|---|---|---|---|---|
| A1 多配置真实测量逐次入账 | GPU | `uv run --locked python examples/autotune_smoke.py --output artifacts/t14` | 每个有效配置独立的原始批样本（5 批） | PASS（1024/2048/512 均 measured） | artifacts/t14/autotune-report.json |
| A2 in-place 输入恢复 | GPU | 同上 | in-place 候选场景下 restore 事件 15 次（每批一次） | PASS | 同上 |
| A3 winner 冻结后复验 | GPU | 同上 | winner（block=2048）frozen_verified=True | PASS | 同上 |
| A4 错误/非法配置不能成为 winner | GPU | 同上 | block=3000 结构剪枝；block=1048576 探针资源失败剪枝；均不得 measured/winner | PASS | 同上 |
| A5 审计语义 | CPU | `uv run --locked pytest tests/test_autotune.py` | winner 必须是正确且最快；缺样本/未冻结复验/缺恢复证据均拒绝（8 项） | PASS | artifacts/t1314-cpu |
| R 回归 | CPU | `uv run --locked kernelagent check --output artifacts/t1314-cpu` | 全量检查不回归 | PASS（406 项 403 过 / 3 Windows 专用 skip / 0 fail） | artifacts/t1314-cpu/a4b8ce183ecb439e8147038d2b4a4017/report.json |

## 运行证据

- 代码/输入/环境/协议身份：驱动 `configs/kernelbench/autotune_driver.py`（子进程探针隔离危险配置）；候选 in-place Triton LayerNorm 夹具；镜像 `sha256:cb7a9f4c…`；GPU RTX 4060 Laptop（CDI UUID）。
- 执行命令及退出码：冒烟 exit 0（accepted=True）；A5/R exit 0。
- 原始日志/报告 hash：报告 sha256（见证据索引 target_ubuntu_t14）；R 报告 sha256 `8f518b7582f5033661556c1c565c8572d8e749cc21a99761c6069cd59c929fe9`。
- skip 数量和原因：3 项 Windows Job Object 专用（Linux skip，同前）。
- 活动作业与恢复方式：无持续作业。

## 结论

- 是否满足本包全部必需检查：是。实现记录：巨大 BLOCK 的 Triton 编译会 OOM 整容器——探针移入子进程（被杀仅损失该配置），主进程存活；容器级 OOMKilled 标志在主进程 exit 0 时不再判失败（executor 语义修订，T04 边界测试回归通过）。
- 与设计的偏差及 ADR：无协议偏离；executor OOM 语义修订记录于 container.py 文档串与本卡片。
- 仍未验证的范围：Kernel Tuner/CUTLASS Profiler 路径（T20）；autotune 内部 GPU 时间与 T07 计时的合并对照。
- 下一步：T11/T18/T19。
