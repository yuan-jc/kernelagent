# T19：FlashInfer-Bench 扩展

## 契约

- 状态：ACCEPTED（2026-09-13，目标真机 GPU 验收）。依赖 T01（ACCEPTED）、T07（ACCEPTED）、T08（ACCEPTED）、T09（ACCEPTED）、T10（ACCEPTED）。
- 对应设计章节：§4、§8.3。
- 环境决策：真实 trace 取自上游 commit `40e6ca78…`（sha256 锁定于 configs/flashinfer-bench/trace-manifest.json）；解法为本项目自写的 sm_89 torch 方案（return 式与 DPS 式）；上游 B200-only 示例解法作为 hardware-guard 负例。
- 本包不包含：NCU（T15 已含）、搜索（T16）。

## 验收矩阵

| 检查 ID | 层级 | 精确命令/审查入口 | 预期观察与失败条件 | 实际状态 | 证据路径 |
|---|---|---|---|---|---|
| F1 真实 trace 验证+计时 | GPU | `uv run --locked python examples/flashinfer_bench_smoke.py --output artifacts/t19` | 真实 trace（definition+workload）按 workload 轴生成输入，解法对 reference 验证并计时 | PASS | artifacts/t19/fib-report.json |
| F2 DPS 与返回语义 | GPU | 同上 | return 式与 DPS 式（预分配输出缓冲）均验证正确 | PASS | 同上 |
| F3 guard 实际检查 | GPU | 同上（guard 段） | 上游 B200-only 解法在 sm_89 上被 hardware guard 拒绝（不执行） | PASS | 同上 |
| F4 revision/张量 hash 留档 | REVIEW | 报告字段 | trace commit、definition/solution sha256、输入张量 hash、workload uuid 全部落盘 | PASS | 同上 |
| R 回归 | CPU | `uv run --locked kernelagent check --output artifacts/t19-cpu` | 全量检查不回归 | PASS（406 项 403 过 / 3 Windows 专用 skip / 0 fail） | artifacts/t19-cpu/（hash 见证据索引 target_ubuntu_t19） |

## 运行证据

- 代码/输入/环境/协议身份：适配器 `src/kernelagent/adapters/benchmarks/flashinfer_bench.py`；驱动 `configs/kernelbench/fib_driver.py`；trace commit `40e6ca78…`（3 文件 sha256 锁定）；镜像 `sha256:cb7a9f4c…`；GPU RTX 4060 Laptop（CDI UUID）。
- 执行命令及退出码：冒烟 exit 0（accepted=True）；R exit 0。
- 原始日志/报告 hash：报告 sha256 `491b3c77f776ae6a9b56730d43e06cd45ec3a9ba191e6fdd2335c68bade19f90`；R 报告 sha256 `ac54414bd050ece5e10e6b6467f753bb91192e6091f3e8a041a73e85db1079b1` 之后的最新全量报告见证据索引。
- skip 数量和原因：3 项 Windows Job Object 专用（Linux skip，同前）。
- 活动作业与恢复方式：无持续作业。

## 结论

- 是否满足本包全部必需检查：是。
- 与设计的偏差及 ADR：无。
- 仍未验证的范围：FlashInfer 生产 kernel 库作为强基线的对比（后续优化实验范畴）；trace 仅用首条 workload 条目（43 条中取 uuid 首条，协议显式固定）。
- 下一步：T18 tritonbench 扩展。
