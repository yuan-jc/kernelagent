# 工作包 T02：KernelBench 数据适配

## 契约

- 状态：IN_PROGRESS
- 对应设计章节：§3–4、8.1（基准契约、任务粒度、KernelBench 接法）。
- 依赖及其验收证据：T01 ACCEPTED（`docs/evidence/T01.json`）；T00 ACCEPTED。
- 当前代码版本/工作区状态：main 干净，T01 已推送（0813410）；domain 层就绪，无 adapter。
- 目标行为：从固定 SHA 的 KernelBench 本地快照读取任务（原版 ID/文件名/代码字节），提供冻结开发清单与协议引用；快照完整性可验证；缺失 ID 明确报错；原版协议与扩展规则在数据结构上分离；不维护平行题库。
- 输入、fixture 与协议版本：上游 ScalingIntelligence/KernelBench 固定 commit `423217d9fda91e0c2d67e4a43bf62f96f6d104f1`（本地缓存 `research/sources/ScalingIntelligence__KernelBench`，不随仓库分发）；开发清单取上游 `dataset.py` 中 `LEVEL{1,2,3}_REPRESENTATIVE_IDS`（L1=18、L2=8、L3=9）；CI 测试用内联合成迷你快照 fixture，仅用于验证读取器，不用作任务。
- 修改范围：新增 `src/kernelagent/adapters/benchmarks/kernelbench.py`、`configs/kernelbench/`（快照文件清单与开发清单 JSON）、`research/fetch_kernelbench_problems.py`（获取脚本）、CLI `bench` 子命令、`tests/test_kernelbench_adapter.py`；pyproject sdist 增加 /configs。
- 本包不包含：evaluator、动态加载题目代码、GPU 执行、`OptimizationTask` 的 workload 物化（T05 在 GPU worker 上做）、tritonbench/FlashInfer 适配。
- 需复用的上游入口及版本：仅静态复用上游解析规则（文件名前缀 int、`LEVEL*_REPRESENTATIVE_IDS`）；不 import 上游模块（其 dataset.py 无 torch 依赖但保持零依赖原则，且题目代码执行属 worker）。

## 验收规格（实现前冻结）

| 检查 ID | 层级 CPU/GPU/LIVE_MODEL/REVIEW | 精确命令/审查入口 | 预期观察与失败条件 | 实际状态 | 证据路径 |
|---|---|---|---|---|---|
| 正常路径：清单与读取 | CPU | `uv run --locked kernelagent check --output artifacts/t02`（含 test_kernelbench_adapter.py 正例） | 合成快照上 list/get 返回一致 ID 与源字节；两次加载记录相等；task_id 含 level/problem_id | NOT_RUN | |
| 必要失败路径：缺失与篡改 | CPU | 同上 | 请求不存在 ID 抛 TaskNotFoundError（含可用 ID 提示）；本地文件缺失或字节被改时完整性校验报告明确列出，不静默通过；清单 hash 与文件不符被拒 | NOT_RUN | |
| 必要失败路径：协议分离 | CPU | 同上（test_kernelbench_protocols.py） | 开发清单中 upstream 与 extended 协议分节；extended 为独立列表，读取器拒绝把它们合并为同一协议 | NOT_RUN | |
| 开发清单一致性 | CPU | 同上（test_kernelbench_dev_manifest.py） | `configs/kernelbench/dev-manifest.json` 内部一致：commit/URL/ID 集合合法、与上游 dataset.py 记录的 ID 集合一致、条目含 sha256；真实快照在本地时深度校验全部 ID/字节（本地额外检查） | NOT_RUN | |
| 真实快照完整性（本地） | CPU | `uv run --locked kernelagent bench verify --root research/sources/ScalingIntelligence__KernelBench` | 270 个题目文件 SHA-256 与清单一致；L1/L2/L3 开发子集 ID 全部存在；输出汇总退出码 0 | NOT_RUN | |
| 相关回归 | CPU | 同上（全部既有测试） | T00/T01 行为不变；domain 边界测试仍通过（adapter 不被 domain 导入） | NOT_RUN | |
| lint/格式/打包 | CPU | `ruff check`；`ruff format --check`；`uv build` + wheel import | 无错误；wheel 含 adapters 子包 | NOT_RUN | |
| REVIEW | REVIEW | 对照设计 §4.3、§8.1 审查 | 不修改上游文件；清单为引用上游而非新题库；协议引用含上游文件 SHA | NOT_RUN | |

## 运行证据

- 代码/输入/环境/协议身份：Windows / Python 3.13.3；上游快照 commit `423217d9fda91e0c2d67e4a43bf62f96f6d104f1`；题目清单 `configs/kernelbench/snapshot-files.manifest.json`（sha256 `52b6ae787155e2ae1431f00c9706278a50aadb804c9241314200017a11349b12`，270 文件、425715 字节，逐文件记录 git blob SHA-1 与 SHA-256）。
- 执行命令及退出码：`python research/fetch_kernelbench_problems.py --proxy http://127.0.0.1:7893 --workers 12` → exit 0（270 verified）；修正清单格式后离线重跑 `python research/fetch_kernelbench_problems.py` → exit 0（全部命中本地缓存并复验 blob SHA，无网络访问）。
- 原始日志/报告 hash：抓取脚本 stdout 记录于会话日志；清单 hash 见上；题目原始文件保留在 gitignored 的 `research/sources/`，公开仓库只含 hash 清单。
- skip 数量和原因：0（本检查点尚无 pytest 覆盖，验收表各项仍为 NOT_RUN，见下）。
- 活动作业与恢复方式：无活动作业；数据准备检查点，adapter 实现从"修改范围"所列文件继续。

## 检查点结论（2026-09-12，IN_PROGRESS）

- 已完成：快照题目文件抓取与完整性校验（270/270 通过 blob SHA）；L1/L2/L3 开发子集 35 个 ID 在快照中全部存在（文件名前缀匹配）。
- 未完成：adapter 模块、dev-manifest.json、CLI verify 子命令、全部 pytest 正反例——验收表所有行保持 NOT_RUN，不因数据就绪而提前标注。
- 下一步：实现 `src/kernelagent/adapters/benchmarks/kernelbench.py` 与冻结验收表中其余检查，然后按表逐项验收。

没有实际证据不填写 ACCEPTED；未执行 GPU/真实模型检查不得写为 PASS。
