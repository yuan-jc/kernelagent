# T10：状态机与预算恢复

## 契约

- 状态：ACCEPTED（2026-09-13，目标真机验收：S1/S2 为真实 GPU 容器流程）。依赖 T05（ACCEPTED）、T08（ACCEPTED）、T09（ACCEPTED）。
- 对应设计章节：§7（自动执行状态机与故障恢复）、§12（预算记账）。
- 目标行为：可恢复的实验状态机——动作（评测/计时/确认/修复）以追加事件日志驱动；预算先预留后结算，超预算拒绝启动新动作；中断重启后未完成动作显式标记 interrupted 并可重跑，绝不假成功；cancel 在动作边界生效；每个动作（含修复/autotune 类）都计费。
- 事件日志：workspace 下 JSONL 追加（action_started / action_finished / action_interrupted / budget_reserved / budget_settled / experiment_done），事件含动作名、输入 hash、预算数、结果引用。
- 本包不包含：搜索策略（T16）、具体生成器（T13）、远程 transport（T22）。

## 验收规格（实现前冻结）

| 检查 ID | 层级 | 精确命令/审查入口 | 预期观察与失败条件 | 实际状态 | 证据路径 |
|---|---|---|---|---|---|
| S1 真实流程串接 | GPU | `uv run --locked python examples/orchestrator_smoke.py --output artifacts/t10` | 评测(T05)→计时(T07)→确认(T09) 动作在同一日志下串接完成，每步预算入账 | PASS（四动作全 finished，结算 720 GPU·s） | artifacts/t10/orchestrator-report.json |
| S2 中断重启不假成功 | GPU | 同上 `--resume`（先人为中断首跑） | 首跑中断后日志中该动作= interrupted；resume 重跑该动作并完成；日志不存在 completed-未执行 的记录 | PASS（os._exit(9) 真崩溃 → interrupted → 重跑完成；重跑计费两次=诚实） | 同上 |
| S3 超预算拒绝 | CPU | `uv run --locked pytest tests/test_orchestrator.py` | 预算耗尽后 try_reserve 返回拒绝，循环停止且状态 budget_exhausted | PASS | artifacts/t10-cpu |
| S4 cancel 语义 | CPU | 同上 | 动作边界收到 cancel → 状态 cancelled，已结算费用保留 | PASS | artifacts/t10-cpu |
| S5 每动作计费 | CPU | 同上 | 每个动作都有 reserve+settle 事件；数量对账一致 | PASS | artifacts/t10-cpu |
| S6 日志不可假造完成 | CPU | 同上 | 只有 action_finished 事件的动作视为完成；手工追加伪 finished 会被输入 hash/结构校验暴露（最小防篡改：hash 链） | PASS（伪造 finish 被记 journal_disowned 并强制重跑；篡改历史被 entry/链校验拒绝） | artifacts/t10-cpu |
| R6 回归 | CPU | `uv run --locked kernelagent check --output artifacts/t10-cpu` | 全量检查不回归 | PASS（376 项 373 过 / 3 Windows 专用 skip / 0 fail） | artifacts/t10-cpu/c0f56b5beac64cc89f633dba4a579bc9/report.json |

S1/S2 为真实 GPU 容器流程；其余为 CPU 语义测试。

## 运行证据

- 代码/输入/环境/协议身份：`src/kernelagent/orchestrator.py`（journal hash 链 + Budget/Action/状态机）；GPU 流程为 T05 评测适配器 + T07 计时协议 + T09 确认规则的真实容器执行；镜像 `sha256:cb7a9f4c…`；GPU RTX 4060 Laptop（CDI UUID）。
- 执行命令及退出码：phase1 `--crash-after evaluate` → exit 9（预期崩溃签名）；phase2 `--resume` → exit 0；S3–S6 单测 exit 0（8 项）；R6 exit 0。
- 原始日志/报告 hash：报告 sha256 `e7732ff7106296e3a9064ae35e55172e9e2a3bb583274c7e911f22ae786f563e`；R6 报告 sha256 `0e3b71a31479acd415e2adf044f468dcee0b6af65dc146dc4ec798077f7454c8`；journal 原件在 artifacts/t10/journal.jsonl。
- skip 数量和原因：3 项 Windows Job Object 专用（Linux skip，同前）；无 docker/GPU skip。
- 活动作业与恢复方式：无持续作业；重跑入口为上述两条命令（journal 已完成时 resume 幂等返回 completed）。

## 结论

- 是否满足本包全部必需检查：是。S1–S6 与 R6 全部 PASS；中断重启不假成功（真崩溃 os._exit(9) 验证）、超预算拒绝启动、每动作计费（重跑双倍计费被如实记录）、伪造完成被 disown。
- 与设计的偏差及 ADR：无。
- 仍未验证的范围：远程 transport/作业 ID（T22）；CUDA Graph 协议下的动作未涉及。
- 下一步：G1 阶段门关闭。T15 NCU 证据适配（依赖已满足）与 T11/T12/T13/T14 按 G2 波次推进。
