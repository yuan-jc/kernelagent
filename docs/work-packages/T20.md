# T20：CUDA/CUTLASS 搜索后端

## 契约

- 状态：IN_PROGRESS（2026-09-13 契约冻结；产物已预置，待沙箱恢复后执行验收）。依赖 T04（ACCEPTED）、T07（ACCEPTED）、T10（ACCEPTED）、T13（ACCEPTED）。
- 对应设计章节：§3.3（Kernel Tuner / CUTLASS 的复用边界）、§8.4。
- 环境决策：T20 诊断镜像层 = pinned ADR-0002 评测镜像（`sha256:cb7a9f4c…`）+ `kernel-tuner`（GPLv3，仅容器内运行、不分发、许可单独记录）；CUTLASS v3.5.1 头文件（BSD-3）只读挂载。CUTLASS tarball 下载命令：`HTTPS_PROXY=http://127.0.0.1:7890 curl -fL -o cutlass-3.5.1.tar.gz https://github.com/NVIDIA/cutlass/archive/refs/tags/v3.5.1.tar.gz`（记录 tarball sha256）。
- 本包不包含：LLM 生成（T12/T16）、NCU（T15 已含）。

## 验收矩阵

| 检查 ID | 层级 | 精确命令/审查入口 | 预期观察与失败条件 | 实际状态 | 证据路径 |
|---|---|---|---|---|---|
| K1 CUDA 调优真实运行 | GPU | `uv run --locked python examples/t20_smoke.py --output artifacts/t20` | Kernel Tuner 对 vector-add CUDA kernel 扫 5 个 BLOCK_SIZE 配置并真实测量（逐配置时间入账） | NOT_RUN | |
| K2 winner wrapper 复测 | GPU | 同上 | winner 配置在 wrapper 中重新运行并精确验证（y == a*x+w） | NOT_RUN | |
| K3 CUTLASS 模板路径真实运行 | GPU | 同上 | pinned `cutlass_gemm.cu`（Simt device::Gemm 模板实例）经 nvcc(sm_89) 编译并运行自验证输出 cutlass-ok | NOT_RUN | |
| K4 许可分别记录 | REVIEW | 报告 licenses 段 | kernel-tuner GPLv3 与 CUTLASS BSD-3 分列，不合并声明 | NOT_RUN | |
| U1 判定语义 | CPU | `uv run --locked pytest tests/test_t20.py` | CUTLASS 接受=compiled∧ran∧cutlass-ok；winner=正确最快配置；许可分列 | NOT_RUN | |
| R 回归 | CPU | `uv run --locked kernelagent check --output artifacts/t20-cpu` | 全量检查不回归 | NOT_RUN | |

## 运行证据

- 待沙箱恢复后执行填写（镜像构建、冒烟命令、退出码、报告 hash）。

## 结论

- 待验收。
