# T05：上游基线与原生评测

## 契约

- 状态：ACCEPTED（2026-09-13，目标 Ubuntu + NVIDIA 真机验收）。依赖 T02（ACCEPTED）、T03（ACCEPTED）、T04（ACCEPTED）。
- 对应设计章节：§8.1、§9.1。
- 环境决策：[ADR-0002](../adr/0002-eval-image-and-protocol-references.md)（锁定评测镜像、`utils.py` 协议引用扩展、正确性-only 赛道）。
- 目标行为：pinned 上游 evaluator（`src/kernelbench/eval.py::eval_kernel_against_ref`）在 ADR-0001 容器边界内真实执行；控制端适配器不导入 torch，判定只从上游序列化结果派生。
- 问题类别（8GB 显存约束下选定，三类别 × 正反例 + compile 基线）：
  - l1 p001 方阵矩阵乘（matmul 类）
  - l1 p040 LayerNorm（归一化类）
  - l2 p001 Conv2d+ReLU+BiasAdd（融合卷积类）
- 本包不包含：正式计时协议（T07）、扩展正确性（T06）、性能排名/晋升（T09）。

## 验收规格（实现前冻结）

| 检查 ID | 层级 | 精确命令/审查入口 | 预期观察与失败条件 | 实际状态 | 证据路径 |
|---|---|---|---|---|---|
| E1 正确候选通过 | GPU | `uv run --locked python examples/kernelbench_eval_smoke.py --output artifacts/t05` | 三类别 correct 候选：上游 `compiled & correctness` 为真，适配器判定一致 | PASS（matmul/layernorm/conv2d-relu-biasadd 全 pass=True） | artifacts/t05/kernelbench-eval-report.json |
| E2 错误候选失败 | GPU | 同上 | 三类别 wrong 候选（B 转置 / 跳过归一化 / 丢弃 bias）：上游 correctness 为假，判定一致 | PASS（三者 pass=False，均被拒） | 同上 |
| E3 compile 基线通路 | GPU | 同上 | torch.compile 候选经上游 evaluator 正确性通过 | PASS（pass=True；需 /tmp tmpfs exec，见 ADR-0001 修订二） | 同上 |
| E4 配置与身份显式记录 | REVIEW | 检查报告字段 | 每用例记录：commit、镜像 ID、GPU UUID、problem/candidate sha256、backend、seed、trials、staged evaluator 文件 hash；结果为 correctness-only（计时属 T07） | PASS | 同上 |
| E5 适配器与上游一致 | CPU | `uv run --locked pytest tests/test_kernelbench_eval_adapter.py` | 缺失/非法上游结果不得成为 pass；判定=compiled & correctness；控制端无 torch/triton 导入 | PASS（9 项单测） | artifacts/t05-cpu |
| R2 回归 | CPU | `uv run --locked kernelagent check --output artifacts/t05-cpu` | 全量检查不回归（新增适配器单测纳入） | PASS（343 项 340 过 / 3 Windows 专用 skip / 0 fail） | artifacts/t05-cpu/18856d2c6a5848c0ba5bb8e77add363e/report.json |

GPU 行必须真实设备运行（容器边界 + `--network none`），CPU 单测不可替代。

## 运行证据

- 代码/输入/环境/协议身份：适配器与驱动见 Git log（db7e4a1 契约+实现）；评测镜像 `kernelagent-eval` image ID `sha256:b598274a22a0…`（ADR-0002 Dockerfile 构建，含 litellm 1.53.1 修复）；GPU RTX 4060 Laptop（CDI UUID 分配）；协议 upstream_compatible（measure_performance=False），上游 commit `423217d9…`，`bench verify` PASS。
- 执行命令及退出码：GPU 冒烟 exit 0（7/7 accepted）；R2 exit 0；E5 exit 0。
- 原始日志/报告 hash：报告 sha256 `5d59e859e9ee71b4dd483605ce93b2869918d9cb41737650d754f90cdac568e8`；CPU 回归报告 sha256 `5bb1729a1baaed7be9891259193c95fa728c232348ab33a8b938ddbf004cc7ee`；各用例原始容器日志与 container-record.json 在 artifacts/t05/workspace 对应 workdir。
- skip 数量和原因：3 项 Windows Job Object 专用（Linux skip，同前）；本机无 docker/GPU skip，容器测试全量真实运行。
- 活动作业与恢复方式：无持续作业；重跑入口为上述两条命令。

## 结论

- 是否满足本包全部必需检查：是。E1–E5 与 R2 全部 PASS；GPU 用例全部经容器边界在真实设备运行且判定与上游一致。
- 与设计的偏差及 ADR：ADR-0002（评测镜像 + `utils.py` 协议引用扩展——仅恢复 pinned evaluator 可运行性，题集/容差/计时不变）；ADR-0001 修订二（/tmp tmpfs 显式 exec，triton/inductor JIT 需要；详见 ADR）。
- 仍未验证的范围：正式计时（T07）；上游 eval 的性能测量路径未激活；多 GPU/并发调度未声明。
- 下一步：T06 增强正确性 / T07 正式计时协议（均依赖 T05，按计划推进）。
