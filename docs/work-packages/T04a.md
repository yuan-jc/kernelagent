# T04a：已有进程机制及复核边界

状态：ACCEPTED（2026-09-13，目标 Ubuntu 复核通过）。它是 T04 的离线子包，不能替代父包验收。

接口：`WorkerRequest -> execute -> WorkerOutcome`。具备私有随机目录、严格输入、环境过滤、日志有界读取、超时和错误结果、目录清理/保留策略。Windows 使用挂起启动、Job 归属后恢复；POSIX 使用独立进程组及回收。completed 只表示进程退出，不是评测 PASS。

## Ubuntu 复核记录（2026-09-13）

- `uv run --locked kernelagent check --output artifacts/worker-cpu` → exit 0：315 项 = 312 passed / 3 skipped（tests/test_worker_boundary.py 的 Windows Job Object 专用路径）/ 0 failed。报告 `artifacts/worker-cpu/31bd5d46653f4f8c82ba25207f8a708a/report.json`，sha256 `f5f98fb051ff539efe442fe6b7987f39d978ff7c89bf2cb1832fef9a984844b8`。
- `uv run --locked python examples/worker_smoke.py` → exit 0，status=completed。
- 卡片"必要回归"清单（路径遍历/可变输入拒绝、缺失 executable、Job 创建/绑定失败、父提前退出后回收后代、超时终止失败、部分日志打开失败、日志回读失败、目录清理失败）均包含在 tests/test_worker_boundary.py，由上述 check 运行覆盖。
- 本包仍只覆盖进程机制：POSIX setsid 逃逸、文件权限/网络/GPU 资源边界不由本包提供，由 T04 容器/cgroup 防线实现。

## CPU 复核入口（保留）

```bash
uv run --locked kernelagent check --output artifacts/worker-cpu
uv run --locked python examples/worker_smoke.py
```
