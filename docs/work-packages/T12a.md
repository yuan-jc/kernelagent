# 工作包 T12a：模型客户端离线层（T12 拆分子包）

## 契约

- 状态：ACCEPTED（本地 CPU 全部检查通过；LIVE_MODEL 属父包 T12 验收范围）
- 对应设计章节：§5–6（可替换 ModelClient、生成器输入）、§12（模型预算入账）。
- 依赖门说明：父包 T12 依赖 T04/T10（均被 GPU 链阻塞）。本子包范围（客户端契约、结构化解析、成本入账、有限重试、录制回放）不依赖 worker 或状态机，经用户同意先行推进；**父包 T12 的验收条件原样保留**——真实模型→代码→GPU 闭环仍需 T04/T10 与真实凭据。
- 依赖及其验收证据：T01 ACCEPTED；T08 ACCEPTED。
- 当前代码状态：main，T03 已推送（937b564，READY_FOR_ACCEPTANCE）。
- 目标行为：`ModelClient` Protocol 与冻结的 `ModelRequest`/`ModelResponse`/`ModelUsage` 契约（规范化 JSON 内容 hash 绑定请求身份）；`ScriptedModelClient`（按序回放）与 `RecordedModelClient`（按 request hash 精确回放，未命中明确报错）；`RetryingModelClient`（仅瞬时错误有限重试+指数退避，永久错误立即抛出）；`CostLedger`/`TokenBudget`（按模型入账、上限守卫）；`parse_structured`/`extract_json_payload`（围栏剥离 + 必填键/类型校验，四类失败显式 ParseError）；全部离线、零第三方运行时依赖。
- 输入、fixture 与协议版本：客户端协议 `model-client-v1`；测试内联构造请求/响应与脚本队列；回放键为 request 内容 sha256。
- 修改范围：新增 `src/kernelagent/adapters/models/`（errors/client/parsing/costing）、`tests/test_model_client.py`；README/看板/交接。
- 本包不包含：真实 HTTP 提供方实现与凭据管理（T12 主体）、提示词模板库（T13/T16）、任何 GPU 执行。
- 需复用的上游入口及版本：无（stdlib）；复用 EvidenceStore（T08）。

## 验收规格（实现前冻结）

| 检查 ID | 层级 CPU/GPU/LIVE_MODEL/REVIEW | 精确命令/审查入口 | 预期观察与失败条件 | 实际状态 | 证据路径 |
|---|---|---|---|---|---|
| 正常路径：客户端流 | CPU | `uv run --locked kernelagent check --output artifacts/t12a`（test_model_client.py） | Scripted 按序返回脚本响应；Recorded 按 request hash 命中（重复命中计数）；Retrying 瞬时错误后成功且退避按注入 sleep 记账（0.5/1.0 指数）；CostLedger 按模型聚合 usage；请求/响应可入 EvidenceStore 且 audit 健康 | PASS | `artifacts/t12a/eb69d7b7c3064bc9b662e1b5d7f70405/`（本地） |
| 必要失败路径 | CPU | 同上 | 脚本耗尽/回放未命中→RecordingMissError（含可用记录数）；瞬时错误超上限→抛最后一次瞬时错误（调用数=max_attempts）；永久错误→不重试立即抛；预算超限→BudgetExceeded；解析失败四类（空/散文/非对象/坏 JSON）+ 缺键/类型错/bool 冒充 int/未知期望类型→ParseError；非法请求构造 10 类被拒 | PASS | 同上 |
| 相关回归 | CPU | 同上（全部既有测试） | T00–T03、T08 行为不变（232 passed 含既有 200）；models 包导入不带入 torch/triton/openai/anthropic | PASS | 同上 |
| lint/格式/打包 | CPU | `ruff check`；`ruff format --check`；`uv build` + wheel 安装 | 无错误；wheel 安装后模型客户端可完成一次 scripted 往返 | PASS | 本地构建输出 |
| REVIEW | REVIEW | 对照设计 §12 审查 | token 成本是一等入账对象；录制响应只用于控制流验证、不冒充生成能力（父包 T12 保留真实闭环验收）；协议版本化（model-client-v1）；请求身份=内容 hash 不含时间戳 | PASS | 本卡片 |

## 运行证据

- 代码/输入/环境/协议身份：Windows / Python 3.13.3 / uv 0.12.13；协议 model-client-v1；验收报告 run_id `eb69d7b7c3064bc9b662e1b5d7f70405`。
- 执行命令及退出码：`uv run --locked kernelagent check --output artifacts/t12a` → exit 0（232 passed/0 failed/0 skipped）；`uv build` + 干净 venv wheel 安装 scripted 往返 → 通过；`ruff check`/`format --check` → exit 0。
- 原始日志/报告 hash：report.json/junit.xml hash 在 report 的 artifacts 段（本地保留）。
- skip 数量和原因：0 skipped。
- 活动作业与恢复方式：无活动作业；离线 CPU 工作。

## 结论

### 2026-09-12 审查修复

- `RecordedModelClient` 在装载与返回时验证 recording key、`response.request_sha256` 和请求模型身份，不能再把 A 请求键指向 B 响应；Scripted 路径使用同一响应身份规则。
- `ModelRequest.messages` 深度不可变，响应 usage/hash/model 字段补齐类型约束；`TokenBudget.ensure_allowed` 只接受非负整数，bool、float 与 NaN 均被拒绝。
- 当前 CPU 证据：269 passed / 0 failed / 0 errors / 0 skipped；共享证据为 `docs/evidence/review-fixes-2026-09-12.json`。LIVE_MODEL 仍属父包 T12，未执行。

- 是否满足本包全部必需检查：是。GPU = N/A、LIVE_MODEL = N/A（本子包为离线契约层；真实模型闭环属父包 T12）。
- 与设计的偏差及 ADR：无关键接口偏离。说明：(1) 拆包决策见"依赖门说明"，属进度安排而非接口变更，父包验收条件未削弱；(2) `ModelRequest.request_sha256` 不含 purpose 字段——purpose 用于账务分类，不影响请求身份（测试锁定）；(3) 真实 HTTP 客户端将在 T12 主体实现本 Protocol 并接入凭据，本包不预设提供方。
- 仍未验证的范围：真实模型调用、结构化解析对真实模型输出的适配（父包 T12）；解析器当前只支持扁平必填键校验，嵌套 schema 留待使用方需求出现时扩展。
- 下一步：真实 Ubuntu/GPU 环境就绪后验收 T03 → 推进 T04。

没有实际证据不填写 ACCEPTED；未执行 GPU/真实模型检查不得写为 PASS。
