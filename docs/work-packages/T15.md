# T15：NCU 证据适配

## 契约

- 状态：ACCEPTED（2026-09-13，目标真机验收）。依赖 T03（ACCEPTED）、T04（ACCEPTED）、T08（ACCEPTED）。
- 对应设计章节：§10.2（NCU 接入策略）。
- 环境决策：[ADR-0003](../adr/0003-ncu-diagnostic-profiling-lease.md)（父端自制固定负载的提权诊断租约；不可信候选绝不进入该路径）。
- 目标行为：真实 `.ncu-rep` 生成 → 宿主无 GPU/无提权导入解析 → 逐 launch 指标、launch 关联（多 kernel 不只取最后一个）、缺失指标显式缺席（绝不补零）、来源标签 `ncu_profile` 与 T07 计时严格分离、权限/架构不支持显式状态。

## 验收矩阵

| 检查 ID | 层级 | 精确命令/审查入口 | 预期观察与失败条件 | 实际状态 | 证据路径 |
|---|---|---|---|---|---|
| N1 真实报告生成 | GPU | `uv run --locked python examples/ncu_evidence_smoke.py --output artifacts/t15` | 诊断租约内真实生成 .ncu-rep（saxpy+2×scale 二进制） | PASS（172166 字节真实报告） | artifacts/t15/t15_two_kernels.ncu-rep |
| N2 导入解析 | CPU | 同上（导入在宿主普通用户运行） | `ncu --import --csv --page raw` 解析出 3 个 launch | PASS | artifacts/t15/ncu-evidence.json |
| N3 launch 关联 | CPU | 同上 | saxpy=1、scale=2 按 kernel 名+顺序关联，不只取最后一个 | PASS | 同上 |
| N4 缺失指标≠0 | CPU | `uv run --locked pytest tests/test_ncu_evidence.py` | 未收集指标=MISSING，显式列入 missing 清单 | PASS（9 项单测） | artifacts/t15-cpu |
| N5 权限/架构显式 | CPU/GPU | 同上 | ERR_NVGPUCTRPERM → gpu_counter_permission_denied（本机非 root 与默认容器实测命中并记录）；CC 8.9 入证据 | PASS | 同上 + ADR-0003 |
| N6 来源分离 | CPU | 同上 | 证据 source=ncu_profile；与 T07 timing-v1 标签互斥 | PASS | artifacts/t15-cpu |
| R 回归 | CPU | `uv run --locked kernelagent check --output artifacts/t15-cpu` | 全量检查不回归 | PASS（385 项 382 过 / 3 Windows 专用 skip / 0 fail） | artifacts/t15-cpu/f12a0364b3a14ce2a8bf083188a10b25/report.json |

## 运行证据

- 代码/输入/环境/协议身份：解析器 `src/kernelagent/adapters/profiling/ncu.py`；NCU 2025.3.1.0（宿主 CUDA 13.0.2 工具链）；镜像 `sha256:cb7a9f4c…`；GPU RTX 4060 Laptop（CDI UUID）；固定负载 two_kernels（本包提交内嵌源码，nvcc -arch=sm_89）。
- 执行命令及退出码：冒烟 exit 0（accepted=True）；R exit 0。
- 原始日志/报告 hash：证据 JSON sha256 `15d39a86709667cd1b190589606174834c1f58e8bfa44cd7c100a203d09a35fd`；R 回归报告 sha256 `e47a51d4e7c67d51e749f91b8d914a5e3565fadb62211f71a6d1e79760f36a9d`；.ncu-rep 172166 字节（hash 入证据 JSON `report_sha256`）。
- skip 数量和原因：3 项 Windows Job Object 专用（Linux skip，同前）。
- 活动作业与恢复方式：无持续作业；重跑入口为冒烟命令。

## 结论

- 是否满足本包全部必需检查：是。N1–N6 与 R 全部 PASS。
- 与设计的偏差及 ADR：ADR-0003 提权诊断租约（宿主 NVreg 计数器权限待用户 sudo 配置，非 root/默认容器实测被拒；租约仅限父端自制固定负载）。
- 仍未验证的范围：`--set full` 全指标集、NSYS/SASS（T17）、用户开启计数器权限后的非特权路径降级。
- 下一步：G2 波次 T11/T12/T13/T14。
