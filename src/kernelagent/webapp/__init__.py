"""Local web console for the optimization loop (v1 Alpha UI).

Served by the Python standard library only - the repository keeps zero
runtime dependencies. The provider API key is filled in the browser,
held in memory on the control plane for the duration of one run, and is
never persisted, logged, or forwarded to any worker container."""
